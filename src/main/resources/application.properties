#Se define el puerto por donde escucha el servidor web
server.port=80

#Se establece que las páginas NO estén en el cache del servidor (en producción SI deben estar en el cache)
spring.thymeleaf.cache=false
#Las páginas no las carga en memoria con este método.
